Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)

	ЛогикаЛогистики.РассчитатьПлановыеДаты(ЭтотОбъект);
	ЛогикаЛогистики.РассчитатьСтоимостьОтправки(ЭтотОбъект);
	ЛогикаЛогистики.ПроверитьПравилоФактБольшеПлана(ЭтотОбъект, Отказ);

КонецПроцедуры

Процедура ПриПроведении(Отказ, РежимПроведения)

	ЛогикаЛогистики.СоздатьЭтапыМаршрута(ЭтотОбъект);
	ЛогикаЛогистики.ЗаписатьСобытиеТрекинга(
		Ссылка,
		Перечисления.ТипСобытияТрекинга.Создано,
		"Создана отправка",
		"",
		Комментарий);

КонецПроцедуры
